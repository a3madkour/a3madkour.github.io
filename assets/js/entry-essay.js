// Essay-section entry — loaded only on /essays/ list + post pages.
// essay.js owns its own selector guards, so it's safe even on the section
// list page where some selectors don't match.
import './essay.js';
