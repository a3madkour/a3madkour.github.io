---
name: project_recipe_section
description: Recipe section feature — brainstormed + filed as a 3-slice design stub; each slice needs its own future session
metadata: 
  node_type: memory
  type: project
  originSessionId: 48dac829-da73-424f-aed9-384f02f41925
---

**2026-07-06 brainstorm → filed as STUB.** New "recipe" feature for the Hugo site. Spec: `docs/superpowers/specs/2026-07-06-recipe-section-design.md` (committed `bcb156d`). **Nothing implemented — every slice gets its own future brainstorm → spec → plan.**

**Core architectural move:** a recipe is a **portable standard artifact**, not locked in the site. Interchange standard = **schema.org/Recipe JSON-LD** (researched: it's the dominant real standard; JSON; `HowToStep` + `Clip.startOffset` gives video-sync natively; the `<head>` JSON-LD block IS the download payload — site has no JSON-LD today, this adds the first). The site is one consumer/contributor; a future separate project is the other.

**ci-local follow-up fixes (2026-07-08, on master, still unpushed):** running `tools/ci-local.sh` after merge caught 2 new-section-infra-drift gaps the per-task E2E/smoke missed → **`f7c7370`** (scope RECIPE output to `target: {kind: page}` so the section page doesn't warn "no layout for recipe/section" + emit a bogus section JSON; add section pagefind-meta spans to `recipes/list.html`; register `/recipes/` prefix in `check_pagefind_meta.py` +2 tests) and **`c039a7f`** (add `/recipes/` = 200K to `check_page_weights.py` BUDGETS_PREFIX — shared ~115K main.css + 16K scaler bundle blew the 100K default). After both: ci-local fully green except the expected local LHCI "no chromium on PATH" stop; E2E 12/12. **Lesson: adding a Hugo section needs entries in check_pagefind_meta (SECTION_BY_PREFIX) AND check_page_weights (BUDGETS_PREFIX) — the plan missed both.**

**SLICE 1 SHIPPED (2026-07-07, merged to master `6d8cf4a`, NOT pushed).** Subagent-driven execution of `docs/superpowers/plans/2026-07-06-recipe-slice-1.md` (11 TDD tasks + final-review fixes, 20 commits). Delivered: `content/recipes/` section, two-column render (sticky ingredients rail + steps, grouped-ingredient sub-headings, dark-mode-safe CSS §50), client-side portion scaling (two-way synced serves/multiplier, unit-aware fraction formatting via pure `recipe-scale.js`), sources list, schema.org/Recipe JSON-LD (site's first) doubling as a downloadable `.json` via a `RECIPE` Hugo output format (shared partial `partials/recipes/schema-recipe.html`), filterable index (Cuisine/Course/tags — **Time dim dropped**, filter-chips is exact-match only), nav "Recipes" (after Streams), 2 linter pairs (34th/35th), new `tests/unit/` node --test JS layer, recipe E2E. Green: linters, node 3/3, CSS/config guards, minified smoke 13 URLs, E2E 3/3. Review caught+fixed: servings crash+Pyright, dark-mode hardcoded-hex, a CI `node --test` dir-form bug (→glob), and 3 whole-branch Importants (grouped ingredients unrendered, missing search chip, schema `image`). **Deferred within slice 1:** synced-video steps = slice 1c (`video` id carried+lint-validated but unrendered; `[mm:ss]` stripped from steps). **Contract note for slice 2:** the on-page render is a SUBSET of the frontmatter contract — `image` emitted-to-schema-only, `video` carried-unused, `group` now rendered; ingredients/sources are flow-style; parser returns floats/null as strings (Python side).

**Three slices (decreasing readiness):**
1. **Site render + download** — *SHIPPED (see above).* `content/recipes/` section, minimal house-style render, sources list (req 1), "Download recipe" button = export (req 3), `[mm:ss]` steps + per-page scaling as follow-ons. Fixtures = hand-authored dummy recipes; does NOT depend on slice 2. Reuse: library umbrella, streams YouTube embed, synced-poetry runtime (media-agnostic → YouTube), explorables (scaling UI), citations (sources), filter-chips, streams linter-pair pattern.
2. **Org authoring + lint + export** — *brainstorm-later, RESEARCH-GATED.* The follow-up the author explicitly deferred. **First research what org offers OOTB for recipes** (org tables, drawers, existing org recipe packages, Cooklang-for-org) before inventing syntax. Then: authoring convention (adding a step = adding a list item; ingredients = org table qty/unit/item/alt), a dotfiles handler `a3madkour-publish-recipes.el` (peer of garden/library/research) emitting Hugo bundle + schema.org JSON + structured ingredients, and a linter guaranteeing clean valid export. **Resolves the wrinkle:** schema.org `recipeIngredient` is free-text strings only — structured qty/unit for scaling (req 6) + aggregation (req 7) must ride alongside.
3. **Meal-prep planner** — *PARKED (2026-07-14), SEPARATE project.* Explicitly set aside by the author — not part of the current recipe work; revisit only when deliberately reopened. The stateful "both consumer and contributor" app (req 7): assemble recipes → aggregate ingredients → shopping list. App-like (persistent client state); outside the static-site design. Reuses garden path-log localStorage-accumulation pattern.

**Locked decisions:** standard = schema.org/Recipe; org-table ingredients; `[mm:ss]` steps reusing synced-poetry grammar; minimal render; sources always listed; download = export; meal-prep is separate.

Follows feedback [[feedback_design_batch_no_plan_until_implement]] + [[feedback_file_for_another_slice_means_stub]] + [[feedback_close_architecture_before_visual]] (architecture closed; layout/wireframes deferred to slice-1 session).
